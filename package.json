{
  "name": "mortgage-intake-agent",
  "version": "1.0.0",
  "description": "AI-powered Mortgage Intake Agent using Gemini + Intentyfi",
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production node server/server.js",
    "start:staging": "cross-env NODE_ENV=staging node server/server.js",
    "dev": "concurrently \"cross-env NODE_ENV=local node server/server.js\" \"vite --mode local\"",
    "dev:client": "vite --mode local",
    "dev:server": "cross-env NODE_ENV=local node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-auth-library": "^10.6.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0",
    "vite": "^6.2.0"
  }
}
